<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManosLocales"
        tools:targetApi="31">
        <activity
            android:name=".authenticator.rememberpw.RememberPWActivity"
            android:exported="false"
            android:label="@string/title_activity_remember_pwactivity"
            android:theme="@style/Theme.ManosLocales" />
        <activity
            android:name=".mainmenu.MainMenuActivity"
            android:exported="false"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.ManosLocales" />
        <activity
            android:name=".authenticator.SinginActivity"
            android:exported="false"
            android:label="@string/title_activity_singin"
            android:theme="@style/Theme.ManosLocales"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".authenticator.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.ManosLocales" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ManosLocales">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>